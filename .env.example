# HTTP Server Port
# The port on which the Express.js server will listen
# Default: 3000
PORT=3000

# Node Environment
# Controls environment mode (development, production, test)
# Default: development
NODE_ENV=development

# Usage Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Edit .env with your local configuration values
# 3. The .env file is git-ignored and safe for local secrets
# 4. server.js reads these values via process.env
